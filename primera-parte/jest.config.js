module.exports = {
  testEnvironment: "node",
  verbose: true,
  rootDir: ".", // Raíz del proyecto
  testMatch: ["**/tests/**/*.test.js"], // Encuentra los archivos de prueba en cualquier subdirectorio de tests
};
